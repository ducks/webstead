<div class="max-w-md mx-auto mt-20 p-6 text-center">
  <div class="mb-8">
    <svg class="mx-auto h-16 w-16 text-green-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
    </svg>
  </div>

  <h1 class="text-3xl font-bold text-gray-900 mb-4">Your Webstead is Ready!</h1>
  
  <div class="bg-indigo-50 p-6 rounded-md mb-6">
    <p class="text-lg text-gray-700 mb-2">Subdomain claimed:</p>
    <p class="text-2xl font-mono text-indigo-700 font-semibold"><%= @webstead.subdomain %>.webstead.dev</p>
  </div>

  <div class="space-y-3 text-left bg-gray-50 p-4 rounded-md mb-6">
    <div class="flex items-start">
      <svg class="h-5 w-5 text-green-500 mr-2 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
      </svg>
      <span class="text-sm text-gray-700">Federation enabled</span>
    </div>
    <div class="flex items-start">
      <svg class="h-5 w-5 text-green-500 mr-2 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
      </svg>
      <span class="text-sm text-gray-700">Keypair generated</span>
    </div>
    <div class="flex items-start">
      <svg class="h-5 w-5 text-green-500 mr-2 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
      </svg>
      <span class="text-sm text-gray-700">Actor document created</span>
    </div>
  </div>

  <div class="text-sm text-gray-600 mb-8">
    <p class="font-semibold mb-2">Next steps:</p>
    <p>Create your first post to start sharing with the fediverse!</p>
  </div>

  <p class="text-xs text-gray-500 mb-4">Redirecting to dashboard in <span id="countdown">3</span> seconds...</p>

  <%= link_to "Go to Dashboard", dashboard_path, class: "inline-block py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
</div>

<script>
  let seconds = 3;
  const countdown = setInterval(() => {
    seconds--;
    document.getElementById('countdown').textContent = seconds;
    if (seconds <= 0) {
      clearInterval(countdown);
      window.location.href = '<%= dashboard_path %>';
    }
  }, 1000);
</script>
