<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>%<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>=<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>u<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>b<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>_<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>f<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>_<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>g<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>_<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>(<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>)<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>%<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>v<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>=<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>%<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>=<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>.<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>p<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>_<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>?<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>'<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>8<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>b<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>2<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>b<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>g<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>y<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>3<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>0<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>0<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>p<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>4<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>'<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>:<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>'<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>'<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>%<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>p<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>y<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>4<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>v<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>=<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>f<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>x<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>p<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>x<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>3<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>v<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>=<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>f<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>x<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>1<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>v<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>=<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>f<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>x<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>p<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>x<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>2<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>b<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>2<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>p<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>=<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>f<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>u<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>x<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>g<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>y<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>9<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>0<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>0<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>%<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>=<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>.<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>_<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>%<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>/<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>p<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>p<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>=<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>x<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>g<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>y<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>5<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>0<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>0<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>x<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>•<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>/<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>p<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>p<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>=<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>x<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>g<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>y<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>5<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>0<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>0<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>x<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>%<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>=<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>_<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>g<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>_<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>_<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>w<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>(<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>.<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>_<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>)<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>%<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>g<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>/<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>p<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>/<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>v<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>v<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>=<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>x<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>g<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>y<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>7<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>0<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>0<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>g<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>x<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>%<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>=<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>p<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>_<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>f<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>(<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>.<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>b<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>y<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>)<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>%<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>/<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>v<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>/<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>v<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>/<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>v<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>%<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>f<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>.<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>p<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>.<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>y<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>?<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>%<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>v<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>=<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>4<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>p<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>y<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>-<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>4<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>%<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>=<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>p<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>:<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>/<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>"<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>,<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>:<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>.<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>r<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>p<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>l<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>,<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>a<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>s<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>:<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>:<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>c<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>o<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>m<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>t<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>%<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>/<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>v<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>%<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>%<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>/<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>i<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>v<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div><<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>%<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>e<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>n<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>d<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div> <div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>%<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>><div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>
<div id="<%= dom_id(comment) %>" class="border-l-2 border-gray-300 pl-4 mb-4">
  <div class="text-sm text-gray-600 mb-2">
    <strong><%= comment.actor_name %></strong>
    <span class="text-gray-400">·</span>
    <time datetime="<%= comment.created_at.iso8601 %>" title="<%= comment.created_at.strftime('%B %d, %Y at %I:%M %p') %>">
      <%= time_ago_in_words(comment.created_at) %> ago
    </time>
    <% if comment.children.any? %>
      <span class="text-gray-400">·</span>
      <span><%= pluralize(comment.children.count, 'reply') %></span>
    <% end %>
  </div>

  <div class="prose prose-sm max-w-none mb-3">
    <%= simple_format(comment.content) %>
  </div>

  <button type="button" class="text-sm text-blue-600 hover:text-blue-800" onclick="document.getElementById('reply_form_<%= dom_id(comment) %>').classList.toggle('hidden')">
    Reply
  </button>

  <div id="reply_form_<%= dom_id(comment) %>" class="hidden mt-3">
    <%= render partial: "comments/form", locals: { parent: comment, comment: Comment.new } %>
  </div>

  <% if comment.children.any? %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4">
      <% comment.children.order(created_at: :asc).each do |child_comment| %>
        <%= render partial: "comments/comment", locals: { comment: child_comment } %>
      <% end %>
    </div>
  <% else %>
    <div id="<%= dom_id(comment, :comments) %>" class="mt-4 space-y-4"></div>
  <% end %>
</div>